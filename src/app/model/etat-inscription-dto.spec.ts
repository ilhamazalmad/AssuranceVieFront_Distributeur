import { EtatInscriptionDto } from './etat-inscription-dto';

describe('EtatInscriptionDto', () => {
  it('should create an instance', () => {
    expect(new EtatInscriptionDto()).toBeTruthy();
  });
});
